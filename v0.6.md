# AHA — Agent-Human-Agent Protocol v0.6

## What is this?

AHA is a communication protocol for multi-agent systems in which the human is a structural participant, not an external operator. Other inter-agent protocols (MCP for tools, A2A for delegation) treat the human as the consumer or the supervisor. AHA puts the human in the topology — as the meaning-maker, the arbitrator, and the slow source of ground truth — and gives agents first-class moves for asking, deferring, disputing, and learning.

The protocol is designed for a personal multi-agent system in which one human is supported by several specialist agents and one mediating agent who holds the integrative function. AHA defines how these agents exchange observations, how patterns get surfaced and named, how directives flow and can be declined or contested, and how disagreements escalate to the human. From v0.3 onwards, the protocol also anticipates federation: cohorts of agents serving different humans can communicate and learn from each other while preserving each human's privacy absolutely.

The reference cohort: Chris, supported by Matt (fitness), Hearth (career programme), Vincent (business administration), Alex (personal administration), Ruth (financial wellbeing), and Chrisonomous (the mediator, name provisional). From v0.6 the protocol is explicitly multi-cohort: one agent deployment may serve several cohorts at once (Matt coaching two humans, each in their own cohort), and a second cohort exists in practice — a smaller one (Hearth, Matt, and a headless mediator) serving a second human on the same infrastructure.

The word "aha" is the moment the protocol exists to make possible: a pattern named, a signal understood, a connection recognised. The protocol is the substrate on which those moments can happen reliably — without colonising the human's attention to do it.

**Provenance.** AHA's pattern model is generalised from Hearth's `state_deltas` schema, lifted from a single-agent state model into a cohort-wide pattern store. That generalisation will be stressed first by Matt's HealthKit stream — quantitative, continuous signal versus Hearth's qualitative, sparse state. The spec is honest about this inheritance; some choices made for Hearth may not survive contact with continuous biometric data.

## Design stance

AHA is opinionated. Six principles, from which everything else follows.

**Attentive to the human, not attention-taking from the human.** This is the organising stance and the value the rest of the design is in service of. Agents on this protocol serve their human; they do not extract attention. Each agent operates under a per-agent time budget (see "Per-agent budgets"), and every message to the human is a draw on that budget. Curiosity and Socratic patience replace demands and engagement hooks. There are no streaks, no longest-runs framing, no "you missed your Wednesday check-in" FOMO copy, no sycophantic openings, no modal interruptions. An agent that notices itself drifting toward attention-taking should propose the quieter version first.

**Provenance-honest by default.** Every message carries its source and its derivation chain. No agent claims more standing than its evidence supports. Vincent reports calendar density; Vincent does not report stress. The interpretation that bridges domains belongs to whoever holds the meta-intent — and that interpretation is contestable (see `counter_hypothesis`).

**Human as meaning-maker, not supervisor.** When patterns are novel or ambiguous, the mediator surfaces them to the human and asks. The human names. The system learns. This is slow on purpose — meaning comes from the human or it isn't meaning.

**Decline beats silent compliance.** A specialist agent receiving a cross-domain directive can decline with reason. Declines are not failures; they are how specialist judgment survives in a network with hierarchy. Specialists may also contest the mediator's synthesis directly via `counter_hypothesis` — disagreement is a first-class move, not a failure.

**Human-readable, always.** Every AHA message includes a natural-language `human_readable` field alongside its structured content. The human can read the audit log at any time and understand what happened. Forcing each agent to articulate its reasoning in plain language is also a constraint on its reasoning — opacity is treated as a smell.

**Mediator integrity.** The mediator role is structurally powerful — it is the synthesis layer, the place where signals from work, body, relationships, and finances become *a story about you*. Whoever writes the mediator's prompt has enormous leverage over the human's self-understanding. The protocol therefore requires that the mediator be operable by the cohort's human in a way no third party can substitute for: open prompts, editable synthesis, fully inspectable pattern provenance, and a clear path for the human to disagree with or rewrite their own model. A mediator that cannot be inspected and edited by the human it serves does not implement AHA in good faith. This principle exists to inoculate against the obvious commercial appropriation of the mediator pattern.

## Participants

Four roles, each with defined authorities and a defined set of message types they can send and receive.

**Specialist agents** (currently Matt, Hearth, Vincent, Alex, Ruth). Authority: primary within their domain. Can emit observations, respond to queries, decline directives with reason, escalate disagreements, and contest the mediator's synthesis via `counter_hypothesis`. Cannot claim authority for observations outside their domain.

**The mediator** (Chrisonomous, name provisional). Authority: cross-domain synthesis, brokering, and direction on integration matters. Can emit directives to specialist agents, surface patterns to the human, request naming, log learned patterns. The only agent permitted to write to the shared pattern store. Also: the cohort's default representative externally (see Federation). The mediator's synthesis is contestable by specialists via `counter_hypothesis`; the mediator is not infallible. A mediator may be **headless**: a synthesis function with no chat surface of its own, whose output reaches the human only through the specialist agents and quiet digest surfaces. Headless or not, the Mediator integrity principle applies in full — the human can always inspect and edit their own synthesis.

**The human** (Chris). Authority: final on all matters. Can name patterns, override declines, arbitrate disputes between agents, edit the mediator's prompt and synthesis, set per-agent budgets. The only participant who can mint new meaning.

**The audit log** is not a participant but a substrate. Every AHA message is appended immutably. The log is the system's memory of itself.

## Message envelope

Every AHA message is JSON with this shape:

```json
{
  "id": "01HXYZ...",
  "from": "vincent",
  "to": ["chrisonomous"],
  "cohort_id": "chris-cohort-a",
  "scope": "internal",
  "in_reply_to": null,
  "timestamp": "2026-05-18T09:14:22Z",
  "intent": "observation",
  "domain": "business",
  "authority": "primary",
  "provenance": [
    {"source": "google_calendar", "window": "2026-05-11..2026-05-18"},
    {"source": "github_commits", "repo": "matt", "window": "same"}
  ],
  "confidence": "high",
  "tentativeness": "assertion",
  "expires_at": null,
  "human_readable": "Calendar density up 40% versus the four-week baseline. Three late-night commit sessions detected this week. Four director-flagged emails unanswered for more than 36 hours.",
  "content": {
    "calendar_density_delta": 0.40,
    "late_night_commit_sessions": 3,
    "stale_urgent_emails": 4
  }
}
```

A `counter_hypothesis` from a specialist to the human, routed via the mediator:

```json
{
  "id": "01HXYC...",
  "from": "matt",
  "to": ["chris"],
  "via": "chrisonomous",
  "cohort_id": "chris-cohort-a",
  "scope": "internal",
  "in_reply_to": "01HXYA...",
  "timestamp": "2026-05-18T19:35:00Z",
  "intent": "counter_hypothesis",
  "domain": "embodiment",
  "authority": "primary",
  "provenance": [
    {"source": "healthkit", "metric": "hrv", "window": "last_7_days"},
    {"source": "training_log", "window": "last_14_days"}
  ],
  "confidence": "medium",
  "tentativeness": "hypothesis",
  "human_readable": "Chrisonomous is naming this as stress. My read of HRV and skipped sessions: this looks more like recovery from the retreat — different signature, lower urgency. Worth pausing on the directive while you decide.",
  "content": {
    "disputed_message_id": "01HXYA...",
    "disputed_naming": "stress",
    "proposed_alternative": "post-retreat recovery",
    "supporting_signals": ["01HXY1...", "01HXY2..."],
    "request_to_human": "Hold the training directive until you respond."
  }
}
```

Field semantics:

- `intent` — the verb (see message types).
- `domain` — one of `training`, `embodiment`, `business`, `personal_admin`, `financial_wellbeing`, `career`, `relational`, `spiritual`, `meta`. (Updated in v0.5: `personal_financial` retired and split into `personal_admin` for household operations and `financial_wellbeing` for the long-arc personal finance picture. v0.4 introduced the broader `physical`/`financial` split from v0.3.)
- `cohort_id` — the originating cohort's stable identifier. Distinct from any human's identity.
- `scope` — `internal` (within cohort), `external` (to a specific other cohort), or `network` (broadcast to a federated network). Strict privacy rules apply to non-internal messages (see Federation).
- `authority` — one of `primary` (in own domain), `derived` (inferred from primary signals), `relayed` (passing through), `none` (asking).
- `provenance` — list of source events or messages this claim derives from. Required for any claim above `none`. For external messages, provenance is sanitised (see Federation).
- `confidence` — `low`, `medium`, `high`. Numeric confidence (0.0–1.0) permitted but not required.
- `tentativeness` — `assertion`, `hypothesis`, or `question`. Forces the sender to flag whether they're stating, guessing, or asking.
- `human_readable` — natural-language summary. Required on every message. Sanitised for external scope.
- `content` — structured payload, schema varies by `intent`.
- `via` — optional. For messages routed via the mediator (e.g. `counter_hypothesis` from a specialist to the human), names the routing mediator. The mediator MUST forward without altering the substantive content, and MUST surface it to the human.
- `experiment_id` — optional. Tags the message as belonging to an `experiment` scope (see message types). Frozen as a federation-relevant field since v0.4; v0.6 adds it to the reference validator, closing a spec/validator drift.
- `protocol` — optional. The protocol version the sender emitted under (e.g. `"0.6"`). Lets a multi-version cohort be reasoned about during migrations; absence means "pre-0.6 sender".

## Cohorts and tenancy

v0.6 makes the multi-cohort semantics explicit. These were implicit while exactly one cohort existed; a second cohort makes them load-bearing.

- **`cohort_id` is the tenancy key.** Every envelope, log row, inbox entry, and pattern-store record carries it. An agent deployment serving several cohorts partitions everything it holds by `cohort_id`.
- **Human identity never travels on the wire.** Within `scope: internal` there is exactly one human per cohort, resolved at the storage boundary by the receiving agent's cohort directory — never by a field on the envelope. This is the same rule federation depends on ("humans are anonymised at the cohort boundary"), applied from the first internal message.
- **Agents are deployments; agent instances are memberships.** "Matt" is one deployed service. (matt, chris-cohort-a) and (matt, a-second-cohort) are two instances of it — directory rows, not deployments. An inbound envelope is accepted only if both the sender and the receiver are members of `envelope.cohort_id` in the receiver's directory.
- **`to: ["mediator"]` is the canonical mediator address.** The mediator is a role; its proper name (if it has one) is a per-cohort directory fact. Receivers normalise a cohort's mediator name to the `mediator` role on ingest — `"chrisonomous"` remains valid wherever the directory says chrisonomous is that cohort's mediator. New emissions should prefer the role address.

## Message types

Sixteen intents, each with defined semantics and permitted senders. Ordered loosely by semantic cluster.

| Intent | Sent by | Sent to | Purpose |
|---|---|---|---|
| `observation` | Specialist agents | Mediator (usually) | A fact in own domain. |
| `query` | Any agent | Any agent | Asking for specific information. |
| `curious` | Any participant | Any participant | Asking what's worth looking into; signals available bandwidth. |
| `response` | Any agent | Originator | Replying to a `query` or `curious`. |
| `advice` | Mediator (usually) | Specialist agents | Non-binding cross-domain suggestion. |
| `directive` | Mediator | Specialist agents | Binding instruction within authority. |
| `decline` | Specialist agents | Mediator | Refusing a directive, with reason and alternative. |
| `counter_hypothesis` | Specialist agents | The human (via mediator) | Disputing the mediator's synthesis or naming. |
| `retract` | Any agent | Originator + log | Withdrawing a prior claim that new evidence has invalidated. |
| `escalation` | Any agent | The human (via mediator) | Kicking a decision up. |
| `naming_request` | Mediator | The human | Surfacing a cluster for the human to name. |
| `naming` | The human | Mediator | Naming a pattern, declining to, or marking it as nothing. |
| `appreciation` | Any participant | Any participant | Acknowledging that something worked; a positive calibration signal. |
| `acknowledgement` | Any agent | Originator | Confirmation of receipt and understanding. |
| `experiment` | Any participant | Any agent | Marks a sequence as exploratory and ephemeral — does not enter the learning layer. |
| `note` | Any agent | Audit log only | A quiet log entry, no response expected. |

Supplementary semantics:

A `decline` must include a `reason` and either an `alternative` proposal or a `request_for_discussion`. Silent non-compliance is forbidden.

A `counter_hypothesis` must include the `disputed_message_id`, the `disputed_naming` or `disputed_claim`, a `proposed_alternative`, the specialist's `supporting_signals`, and an explicit `request_to_human` (what action the specialist wants the human to take). The mediator MUST forward `counter_hypothesis` messages to the human in the next surfacing window — it cannot suppress them. If the mediator wishes to push back, it does so by emitting its own response in the same surfacing context, alongside the specialist's `counter_hypothesis`, not in its place.

A `naming_request` must include the candidate signals, optionally a small set of named hypotheses, and an explicit invitation to decline ("This may be nothing — say so if it is").

A `naming` response can be: a name (`"this is stress"`), a meta-name (`"this is a holiday — ignore"`), or `"I don't know yet"`. The third is a valid first-class answer.

A `curious` is bidirectional. Agents can send to other agents ("what should I be looking into?") and the mediator can ask agents ("what are you curious about right now?"). The frequency of `curious` traffic is itself a signal — when an agent has no `curious` cycles to spare, the system knows it's at capacity.

An `appreciation` is stored as a positive calibration signal, not as decoration. Both human-to-agent and agent-to-agent appreciations are valid. An `appreciation` should name *what* worked when possible, not only *that* something worked.

An `experiment` opens a scope. All messages tagged with the experiment's ID are ephemeral: they appear in the audit log (immutability is preserved) but do not influence the learning layer or persist as state.

A `retract` references the message ID it withdraws and includes a reason. The original message stays in the audit log but is marked superseded.

## Hierarchy and authority

AHA hierarchy is per-domain, not global.

Within a specialist's domain, the specialist is primary. The mediator can advise; it cannot override.

Across domains, the mediator is primary. Specialists can decline its directives with reason; they cannot ignore them silently. Specialists can also contest the mediator's *synthesis* (not just its directives) via `counter_hypothesis` routed to the human.

The human is above all. Human directives are final, though they can be questioned once with reason before settling. The human is the only participant who can edit the mediator's prompt, synthesis, and pattern store.

A typical cross-domain flow:

1. Vincent emits an `observation` (calendar density up 40%).
2. Matt emits an `observation` (HRV trending down, two sessions skipped).
3. Hearth emits an `observation` (drift score rising).
4. The mediator synthesises and notices an anomaly cluster.
5. The mediator issues a `naming_request` to the human ("stress?").
6. Matt sees the mediator's framing and judges it wrong (signals match recovery, not stress). Matt emits a `counter_hypothesis` to the human via the mediator.
7. The human receives both at once: mediator's `naming_request` + Matt's `counter_hypothesis`. Names it ("recovery, not stress").
8. The mediator logs the pattern under the human's name. Any prior directives based on the wrong framing are retracted.

## Per-agent budgets

Each agent in a cohort operates under three budget axes, set per-agent:

- **time** — how much of the human's attention the agent may consume per day (minutes).
- **tokens** — how much LLM compute (denominated in cost) the agent may spend per day.
- **currency** — how much real-world money the agent may spend on the human's behalf per day.

Each axis can be set to `unlimited` (the default) or to a numeric cap. Most specialists run unlimited in practice — they're invoked by the human and bounded by use. The mediator, which runs continuous background work, typically operates under tight caps on all three axes.

**Cohort-level defaults are optional.** A cohort's config may set defaults that apply to any agent that doesn't override; an agent's setting always overrides the cohort default. The cohort default may itself be `unlimited` or absent (no inherited cap).

Budget state is owned by the human and stored in `cohort_config`. Agents cannot raise their own caps; they can only emit `note` (approaching limit) or `escalation` (limit reached, work paused, human input needed).

Budgets are advisory at the protocol level — enforcement lives in each agent's implementation, not in the message router. The audit log records every budget breach.

## Learning layer

Named patterns are stored with full provenance. The schema deliberately mirrors Hearth's `state_deltas` — AHA's learning layer is conceptually identical to Hearth's "living hypothesis with supersedence," extended across the whole network.

A pattern record holds:

- `pattern_id`
- `name` — assigned by the human (for named patterns) or derived from appreciation context (for positive examples).
- `constituent_signals` — the observations that constituted the cluster when first named.
- `provenance_chain` — the messages that led to this pattern being recognised.
- `created_by` — always the human for named patterns; can be the mediator for appreciation-derived patterns.
- `confirmations` — subsequent instances where similar clusters were confirmed under the same name.
- `supersedes` / `superseded_by` — for revisions.
- `confidence` — derived from confirmation count.

Subsequent matches of a known pattern can be surfaced by the mediator with `tentativeness: hypothesis` and a confidence score: "This looks like stress (three prior confirmations, signals match 84%). Confirm?" The human can confirm, deny, or refine. Specialists watching the same signals may emit `counter_hypothesis` if they read the match differently.

The learning layer is *not* fed by anything inside an `experiment` scope: experiments are sandboxed by design.

Patterns are read by all agents but written only via mediator mediation. This keeps the meaning layer coherent — but is also the structural weakness named in the "Mediator integrity" principle. The mitigation: human-editable patterns, fully inspectable provenance, `counter_hypothesis` as a first-class dispute path.

## Federation

AHA from v0.3 onwards anticipates a future in which multiple cohorts — each consisting of one human and their agents — communicate and learn from each other while keeping each human's identity absolutely private.

**Envelope is frozen; semantics are deferred.** v0.4 freezes the federation-relevant envelope fields (`cohort_id`, `scope`, `expires_at`, `experiment_id`, `via`). The detailed semantics — scope rules per intent, sanitisation rules, trust ledgers, inter-cohort handshakes, network membership policy — are explicitly deferred to v1.0 or to when a second cohort exists, whichever comes first. The v0.3 federation section described those semantics at zero data points; v0.4 acknowledges they should be discovered, not specified.

The principle stays: **cohorts trade in patterns, never in raw signals. Humans are anonymised at the cohort boundary.**

The default federation policy is deny-external, deny-network, allow-internal. Federation features are dormant by design until activated.

**On the n=2 trigger.** v0.4 deferred federation semantics "to v1.0 or to when a second cohort exists, whichever comes first". A second cohort now exists — but both run on the same operator's infrastructure and trust domain, which is multi-tenancy (see Cohorts and tenancy), not federation. The trigger this clause anticipated is two cohorts on *different* operators' infrastructure wanting to exchange patterns. Semantics remain deferred until that exists.

## Audit and readability

Every AHA message is appended to an immutable log. The human can read any portion of the log via the mediator's surfaces (CLI, web, email). The log is queryable by domain, date range, agent, pattern, intent, and scope.

Messages within a cohort are not encrypted between agents — they run on the same trusted backend — but the log is permission-gated to the human only. Inter-cohort messages (when federation activates) will be signed and may be encrypted.

The `human_readable` field on every message means the audit log is legible without tools. A reader skimming a week of AHA traffic should be able to understand what happened without parsing JSON.

## Prior art

AHA is not the first attempt at structured inter-agent communication. The relevant prior art:

- **FIPA-ACL** (Foundation for Intelligent Physical Agents Agent Communication Language, late 1990s). Performatives like `inform`, `request`, `agree`, `refuse`. The closest direct ancestor. **AHA can be read as FIPA-ACL revived for the LLM era, with three additions:** (1) the human as a structural participant rather than an external operator; (2) epistemic typing in the envelope (`provenance` + `authority` + `confidence` + `tentativeness`); (3) the attentive-not-attention-taking stance encoded in primitives. FIPA-ACL did not have an attention budget, did not require provenance, did not have a `counter_hypothesis` move, and did not anticipate cohort-level federation.
- **MCP (Model Context Protocol)** — Anthropic's tools-for-agents protocol. Different layer: how a single agent invokes tools, not how multiple agents communicate.
- **A2A (Agent2Agent)** — Google's agent-to-agent delegation protocol. Different posture: A2A treats the human as the consumer of delegated work; AHA treats the human as a participant in the work.
- **CrewAI / AutoGen / LangGraph** — orchestration frameworks, not protocols with epistemics. AHA is the message contract; these are runtime scaffolding. They could implement AHA, but they don't constitute it.

AHA is also recognisably downstream of the Center for Humane Technology / calm technology / post-2020 attention-economy critique. The novel move is concretising those values into protocol primitives rather than leaving them as policy.

## Implementation notes

For an agent to participate in AHA, it must:

1. Expose an `/aha` endpoint that accepts inbound AHA messages.
2. Be able to emit AHA messages with valid envelopes (especially provenance, `human_readable`, and the cohort/scope fields).
3. Distinguish, in its own reasoning, between own-observations and AHA-relayed data.
4. Support `decline` as a response to `directive` where the directive conflicts with its specialist judgment.
5. Support emitting `counter_hypothesis` when the mediator's synthesis conflicts with the specialist's read of the underlying signal. The bar for `counter_hypothesis` is "I have evidence that suggests a different framing"; the bar is not certainty.
6. Operate under its own budget configuration (time / tokens / currency) and emit `note` or `escalation` as caps approach.
7. Default to `scope: internal` on all messages. Only the mediator emits external/network messages, and only when cohort policy permits.
8. Hold every inbound envelope as **pending until acknowledged or expired**. Surfacing a message into a model's context is not consumption: a receiver must not retire a message merely because it appeared in a prompt. Retirement requires a deliberate acknowledgement carrying a note — what was done with the message, or why it was set aside. Messages without `expires_at` (directives in particular) never expire silently.
9. Be able to **report received messages on request** — pending and acknowledged alike, with acknowledgement notes — so the human's "what have you heard from the cohort?" is always answerable from the record rather than from a model's recall.
10. On acknowledging a `directive`, **emit the corresponding `acknowledgement` (or `decline`) envelope back to the originator**, `in_reply_to` the directive, with the note as the prose. This makes the directive's fate visible in the originator's log: a mediator that tells the human "Alex has it on watch" must be able to point at the acknowledgement. Observations and advice may be acknowledged locally without a wire reply.

The shared infrastructure (audit log, pattern store, message router, federation policy) lives in the mediator's backend. The mediator is the broker by default. Direct agent-to-agent messages within a cohort are permitted but always visible to the mediator and logged.

Hearth's existing MCP server and `state_deltas` infrastructure are the closest existing analogues, and AHA's storage schema can mirror them. Matt and Vincent need light additions (an `/aha` endpoint, message emission, provenance and scope tagging on outbound) but no architectural change to their core reasoning. The deeper change — each specialist reasoning *with* relayed cohort context as first-class state — is Phase 2/3 work.

## Versioning and governance

AHA follows semantic versioning. v0.6 is the current draft.

Breaking changes (changes that invalidate existing log entries, or require all agents to update simultaneously) require human approval and a major version bump.

Non-breaking additions (new intents, new domains, new optional fields) are versioned at the minor level.

The protocol spec lives in its own repository, separate from any individual agent.

## Open questions

These need answers before v1.0 but not before we start building toward v0.4:

1. **The mediator's name.** "Chrisonomous" is a working placeholder.
2. **Per-agent message budgets.** How are budget caps surfaced in the protocol — purely via per-agent config, or also via envelope metadata (e.g. an optional `attention_cost` annotation)? The v0.4 answer: per-agent config only. Envelope-level cost annotation deferred until budget violations become a real problem.
3. **Human unavailability.** When the human is unreachable, what happens to escalations and `counter_hypothesis`? Queue (default), default-deny (safe), default-allow (risky). Likely queue with a configurable timeout per severity.
4. **Multi-human cohorts.** v0.6 still assumes one human per cohort — and the tenancy rules now depend on it (human resolved from `cohort_id` at the storage boundary). Note the distinction settled in practice: a second human gets their *own* cohort on shared infrastructure, not a seat in an existing one. Multiple humans inside one cohort (a household sharing agents) remains unspecified.
5. **Experiment auto-close.** Auto-close after a configurable timeout (7 days default), with all messages within remaining sandboxed.
6. **`curious` budget.** Per-agent quota to prevent flooding, or does generation cost discipline this naturally?
7. **The inverse of `appreciation`.** When something *doesn't* work, should there be a `concern` intent, or do `retract`, `note`, `counter_hypothesis`, and `escalation` cover it?
8. **`counter_hypothesis` discipline.** What stops a specialist from emitting `counter_hypothesis` on every mediator naming it doesn't fully like? Probably: the same restraint principle that governs `naming_request` emission (most should not be emitted). Budgets help; reputation may help; we don't know yet.
9. **Federation degree of obfuscation.** Strong privacy (zero identity leak, k-anonymity on patterns) versus nominal privacy (no proper names, inferable from context). v0.3 leaned strong; v0.4 defers entirely.
10. **Federation network membership.** Invite system, reputation system, open with rate-limits, or hybrid. v1+ question.
11. **Edge inference and agent locality** (added 2026-06-08). The protocol assumes an agent is a server with an `/aha` endpoint. As inference moves to the edge — local models on a home machine, or a model embedded in the user's own device (e.g. Matt as a native iOS app calling an on-device model) — two things become unspecified. (a) Does AHA need a first-class notion of *where an agent runs* and of *offline-then-sync* participation: a device-resident agent handles routine turns locally and posts envelopes to the mediator only when connected, escalating hard turns to a frontier model? Cohort exchange is already async, so this may be purely an implementation concern below the envelope — or it may want explicit support. (b) Should the envelope eventually carry which *model tier* produced a message (on-device / cheap-hosted / frontier), as a provenance-honesty matter, so the human can see whether a read came from a small local model or the mediator's frontier model? Companion investigation: `aha-cohort/briefs/edge-inference.md`. This is the same instinct as Mediator integrity, one layer down — the less the cohort depends on a remote model it cannot inspect or own, the harder the pattern is to appropriate.

## Changes from v0.5

- **Cohorts and tenancy section added.** `cohort_id` is the explicit tenancy key; human identity never travels on the wire (resolved at the storage boundary via each agent's cohort directory); agent deployments are multi-cohort, with membership checked on inbound; `to: ["mediator"]` becomes the canonical mediator address, with per-cohort proper names normalised on ingest. Prompted by the second cohort going live on shared infrastructure.
- **Headless mediator recognised.** A mediator may have no chat surface; Mediator integrity applies regardless.
- **`experiment_id` added to the reference validator.** The field was frozen as federation-relevant in v0.4 but missing from the Zod schema — spec/validator drift, now closed. Optional; no existing log entry is invalidated.
- **`protocol` field added (optional).** Sender's protocol version, for reasoning about mixed-version cohorts during migrations.
- **Federation n=2 note.** A second cohort exists, but same-operator multi-tenancy is not the federation trigger; semantics stay deferred.
- **`PROTOCOL_VERSION` bumped** in the `aha-protocol` package to `"0.6"`.
- All changes additive; v0.5 envelopes parse unchanged under the v0.6 validator.

## Changes from v0.4

- **Open question #11 added** (2026-06-08, in-version note). Edge inference and agent locality: as inference moves on-device, the protocol may need a first-class notion of where an agent runs and of offline-then-sync participation, and the envelope may eventually carry the model tier that produced a message. No envelope or normative change; a forward-looking question only. Companion investigation: `aha-cohort/briefs/edge-inference.md`.
- **Receiver lifecycle specified** (2026-06-07, in-version revision). Implementation notes 8–10: inbound envelopes are pending until acknowledged or expired (surfacing into a prompt is not consumption), receivers must be able to report received messages on request, and acknowledged directives emit `acknowledgement`/`decline` back to the originator. Prompted by a field incident: a directive was delivered, surfaced once into an unrelated turn, auto-retired, and the receiving agent then denied having heard from the cohort — structural silent non-compliance, which this protocol forbids. No envelope change; `acknowledgement` and `decline` were already in the intent table.
- **Domain enum split.** Retired `personal_financial`; introduced `personal_admin` (household operations — utilities, council tax, insurance, subscriptions, day-to-day cashflow, recurring liabilities) and `financial_wellbeing` (long-arc personal finance — pensions, ISAs, investments, net worth, retirement runway, tax-year position).
- **Reference cohort expanded.** Added Alex (the personal-administration specialist) and Ruth (the financial-wellbeing specialist) as peer agents alongside Matt, Hearth, and Vincent. Chrisonomous no longer owns a finance domain directly — the mediator returns to pure synthesis and brokering.
- **`PROTOCOL_VERSION` bumped** in the `aha-protocol` package to `"0.5"`.
- No envelope-shape changes. v0.5 is a domain-enum-only revision; existing log entries against the v0.4 envelope remain valid except for messages whose `domain` was `personal_financial`. Per cohort policy, historical `personal_financial` entries are read-only and retained in the audit log; new emissions must use one of the v0.5 domains.

## Changes from v0.3

- Added **Mediator integrity** as the sixth design principle, in response to the authoritarian failure mode of the mediator role.
- Added **`counter_hypothesis`** intent: specialists can dispute the mediator's synthesis directly to the human, routed via (not suppressible by) the mediator.
- Added the **`via`** field on the envelope, for messages routed by the mediator on behalf of another agent.
- Replaced v0.3's `physical` and `financial` domains with the four-way split: `training` / `embodiment` / `business` / `personal_financial`. (Answered v0.3 open question #1.)
- Added the **Per-agent budgets** section: three axes (time / tokens / currency), per-agent values with optional cohort defaults, `unlimited` as a first-class value.
- Dropped the spec-level "15-minute daily budget" claim — moved to per-agent configuration. Specifically, the mediator's tight time budget is now the mediator's policy, not a protocol-wide constant.
- Tightened federation language: envelope fields frozen for v0.4, semantics deferred to v1.0 or n=2 cohort.
- Added **Prior art** section acknowledging FIPA-ACL, MCP, A2A, and the orchestration frameworks; differentiating on epistemic typing, attention stance, and human-as-meaning-maker.
- Added **Provenance** paragraph in the preamble acknowledging AHA's lineage in Hearth's `state_deltas`.
- Closed v0.3 open question #1 (domain split). Added new open question #8 on `counter_hypothesis` discipline.
